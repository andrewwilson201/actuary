% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ml_pattern_range.R
\name{ml_pattern_range}
\alias{ml_pattern_range}
\title{get range of development patterns from fit_development_pattern_ml function}
\usage{
ml_pattern_range(ml_results, num_fits = 25, metric = ave_score)
}
\arguments{
\item{ml_results}{the list object returned by running the fit_development_pattern_ml}

\item{num_fits}{the number of fits to include in the range. e.g. if set to 25 it will take the best 25 fitting patterns from the ml_results list object.}

\item{metric}{which goodness of fit metric to use when selecting the best fits. ave_score, cdr_score, huber_loss, rmse, mae or neg_ave_score.}
}
\value{
a list with a plot showing the range of percentage developed at each development month and a tibble with the monthly pattern for each of the fits.
}
\description{
get range of development patterns from fit_development_pattern_ml function
}
\examples{
\donttest{

# set up a small parameters file for illustration
cl_parameters <- tidyr::expand_grid(smooth_from = 999,
                                    exclude_last_diag = c(TRUE, FALSE),
                                    exclude_high = FALSE,
                                    exclude_low = FALSE,
                                    selected_curve = "weibull",
                                    num_periods = c(3, 5),
                                    future_dev_periods = 0)

# run function (num_cores = 1 for sequential processing)
ml_result <- fit_development_pattern_ml(uw_year,
                                        dev_year,
                                        claim_number,
                                        triangle_data, 1, 12,
                                        cl_parameters,
                                        num_cores = 1)

# get range of top 2 best fitting patterns
ml_pattern_range(ml_result, 2)

}

}
