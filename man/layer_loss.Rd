% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/layer_loss.R
\name{layer_loss}
\alias{layer_loss}
\title{calculate occurrence or aggregate layer losses}
\usage{
layer_loss(
  year,
  loss,
  retention,
  limit,
  reinstatements = 999,
  type = "occurrence"
)
}
\arguments{
\item{year}{the variable in the input data that represents simulation year}

\item{loss}{the variable in the input data that represents the loss amounts that will feed into the layer}

\item{retention}{the retention the layer being priced}

\item{limit}{the limit of the layer being priced}

\item{reinstatements}{the number of reinstatements. defaults to 999}

\item{type}{type of layer. occurence or aggregate. defaults to occurrence.}
}
\value{
losses into the layer
}
\description{
create a column with occurrence or aggregate losses into a layer with specified limit, retention and reinstatements
}
\examples{
\dontrun{

# calculate 10m x 10m layer with 1 reinstatement
losses |> dplyr::mutate(l1 = layer_loss(year, amount, 10e6, 10e6, 1))

# calculate 10m xs 10m agg layer with 0 reinstatements
losses |> dplyr::mutate(l1 = layer_loss(year, amount, 10e6, 10e6, 0, type = "aggregate"))

}

}
